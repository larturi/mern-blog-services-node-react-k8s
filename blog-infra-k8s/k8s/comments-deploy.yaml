apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-comments-service-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comments
  template:
    metadata:
      labels:
        app: comments
    spec:
      containers:
        - name: comments
          image: larturi/comments:0.0.1
